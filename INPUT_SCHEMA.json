{
  "title": "YouTube Channel Scraper: Stats, Videos & Engagement",
  "description": "Scrape public YouTube channel stats and latest videos in fast or detailed mode. Accepts channel URLs, @handles, or search keywords.",
  "type": "object",
  "schemaVersion": 1,
  "required": [],
  "properties": {
    "channelUrls": {
      "title": "Channel URLs",
      "description": "YouTube channel URLs or @handles to scrape, for example https://www.youtube.com/@mkbhd or @mkbhd.",
      "type": "array",
      "editor": "stringList",
      "maxItems": 50,
      "default": ["https://www.youtube.com/@mkbhd"],
      "prefill": ["https://www.youtube.com/@mkbhd"],
      "sectionCaption": "Channel Input",
      "sectionDescription": "Provide YouTube channel URLs or handles. Both full URLs and @handles are accepted."
    },
    "searchKeywords": {
      "title": "Search Keywords",
      "description": "Optional keywords used to find public YouTube channels. Search results vary by region and ranking.",
      "type": "array",
      "editor": "stringList",
      "maxItems": 10,
      "prefill": [],
      "sectionCaption": "Search Input",
      "sectionDescription": "Optional: search YouTube for channels by keyword."
    },
    "mode": {
      "title": "Scrape Mode",
      "description": "Fast reads channel video grids and returns public channel-tab links with minimal requests. Detailed also reads About pages and selected video pages for total views, join date, country, websites, classified social profiles, likes, descriptions, tags, and category. Email addresses are not collected.",
      "type": "string",
      "enum": ["fast", "detailed"],
      "enumTitles": ["Fast - channel stats and video summaries", "Detailed - engagement and About data"],
      "default": "fast",
      "editor": "select",
      "sectionCaption": "Extraction Mode",
      "sectionDescription": "Use Fast for larger monitoring runs. Use Detailed when you need richer research fields."
    },
    "maxChannels": {
      "title": "Max Channels from Search",
      "description": "Maximum number of channels to scrape from each search keyword. Detailed mode allows at most 10 channels total per run.",
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "default": 1,
      "editor": "number"
    },
    "maxDetailedVideosPerChannel": {
      "title": "Videos to Enrich Per Channel",
      "description": "Detailed mode only: number of saved video rows whose individual pages are read for exact views, likes, descriptions, tags, category, and publish date.",
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "default": 1,
      "editor": "number"
    },
    "maxVideosPerChannel": {
      "title": "Max Videos Per Channel",
      "description": "Maximum latest public video rows to save per channel from YouTube's currently loaded public Videos grid. Fewer may be available.",
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "default": 1,
      "editor": "number"
    },
    "includeShorts": {
      "title": "Include Shorts",
      "description": "Include videos detected as YouTube Shorts. Detection prefers YouTube's /shorts/ route and uses duration only as a fallback.",
      "type": "boolean",
      "default": false,
      "editor": "checkbox"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "description": "Optional proxy settings for HTTP requests. Direct mode is the low-cost default; enable Apify Proxy or provide custom proxy URLs if direct requests are rate-limited.",
      "type": "object",
      "editor": "proxy",
      "default": {
        "useApifyProxy": false
      },
      "sectionCaption": "Proxy Settings",
      "sectionDescription": "Direct mode is usually sufficient. Enable a proxy only when needed."
    }
  }
}
